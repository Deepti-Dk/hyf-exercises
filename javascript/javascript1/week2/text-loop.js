//let stringToLog, numberOfTimeToLog;

function stringLoop(stringToLog, numberOfTimeToLog) {
  for (let i = 1; i <= numberOfTimeToLog; i++) {
    console.log(stringToLog);
  }
}

stringLoop('Hello', 10);
